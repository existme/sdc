\subsection{mac}
\paragraph{Find process using the port}

To find the process using a port you can use the following command: \ic{lsof -i tcp:8080} Then you can kill the process using the process id: \ic{kill pid}